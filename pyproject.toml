[project]
name = "cipher"
version = "0.2.0"
description = "Unsupervised cipher cracking using MCMC, HMM, and genetic algorithms"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "rich",
    "requests",
    "matplotlib",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cipher"]

[project.scripts]
cipher = "cipher.cli:main"
